# Adopters

Organizations publishing an Agent Manifest at `/.well-known/agent-manifest.json`.

| Organization | Manifest URL | Date added |
|---|---|---|
| AgentGate | <https://agentgate.lovable.app/.well-known/agent-manifest.json> | 2026-05 |

## Add your org

Open a PR appending a row above. Requirements:

- The URL must return a manifest that validates against `schema/manifest.v1.json`.
- The URL must be publicly fetchable (no auth required to read).
- Use HTTPS.
